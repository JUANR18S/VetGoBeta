<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema base de tu app -->
    <style name="Base.Theme.VetGoBeta" parent="Theme.Material3.DayNight.NoActionBar" />

    <!-- Tema normal de la app -->
    <style name="Theme.VetGoBeta" parent="Base.Theme.VetGoBeta" />

    <!-- *** TEMA DE SPLASH *** -->
    <style name="Theme.VetGoBeta.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.VetGoBeta</item>
    </style>

</resources>
